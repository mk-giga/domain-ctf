const { Character, Player, Entity, Interactable } = require('./character');

class Game {
    static maps = {};
    
    static init() {
        
    }

    static start() {

    }
}

module.exports = { Game };